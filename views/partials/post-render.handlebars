<div class="card col-9 bg-info">
  <div class="card-header bg-secondary">
    <h2><a href="/post/{{id}}">{{post_title}}</a></h2>
    <p>Posted by {{user.username}} on {{format_date created_at}}</p>
  </div>
  <div class="card-body">
    <p class="card-text">{{post_text}}</p>
  </div>
</div>