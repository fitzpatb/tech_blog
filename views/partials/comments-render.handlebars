{{#each this}}
<div class="card col-9 bg-info">
  <div class="card-body">
    <p class="card-text">{{comment_text}}</p>
    <p class="card-text" id="person">-{{user.username}}, {{format_date created_at}}</p>
  </div>
</div>
{{/each}}